type Person {
  id: ID!
  name: PersonName
  ageRange: AgeRange
  homeAddress: Address
}

type PersonName {
  first: String!
  middle: String
  last: String!
}

enum AgeRange {
  CHILD
  TEEN
  ADULT
  SENIOR
}
