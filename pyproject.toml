[project]
name = "vdm"
version = "0.1.0"
description = "Tooling configuration support for VDM"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "s2dm>=0,<1",
]

[tool.uv]
package = false
